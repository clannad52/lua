-- 文件名：test_simple_backend.lua
-- 这是一个极度简化的测试脚本
local module = {}

-- 使用一个固定的、你100%确认可用的代理服务器
-- 请务必替换成你从可靠来源找到的IP和端口
local TEST_SERVER = "180.101.50.208"
local TEST_PORT = 443

function module.process(host, port, headers)
    -- 打印调试信息（虽然不一定能看到，但无害）
    print(string.format("[Test] 尝试连接: %s:%d -> %s:%d", host, port, TEST_SERVER, TEST_PORT))

    -- 注入关键的认证头（根据你的发现）
    headers["X-T5-Auth"] = "ZjQxNDIh"
    -- 也可以尝试添加Host头
    headers["Host"] = "baidu.com"

    -- 返回最简化的连接信息
    return {
        host = TEST_SERVER,
        port = TEST_PORT,
        headers = headers
    }
end

return module